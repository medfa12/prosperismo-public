repos:
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v22.1.3
    hooks:
      - id: clang-format
        name: clang-format (C++)
        files: ^src/.*\.(?:cpp|h|inc)$
        types: [file]
